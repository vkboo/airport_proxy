# 机场订阅代理服务环境变量配置

# 主订阅链接 (必需)
PRIMARY_URL=https://your-primary-subscription-url.com/subscription

# 备用订阅链接 (可选)
BACKUP_URL=https://your-backup-subscription-url.com/subscription

# 服务器端口 (本地开发时使用，生产环境由 Vercel 自动设置)
PORT=3000

# 运行环境 (生产环境由 Vercel 自动设置)
NODE_ENV=development
